import { container } from "../../assets/jss/hackers-inside";

const servicesStyle = {
    section: {
        background: "#EEEEEE",
        padding: "70px 0"
    },
    container,
    textCenter: {
        textAlign: "center"
    }
};

export default servicesStyle;
